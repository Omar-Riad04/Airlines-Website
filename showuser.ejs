<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Users</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            padding: 20px;
        }
        .user-card {
            margin-bottom: 20px;
            padding: 15px;
            background-color: #e0e0e0;
            border-radius: 8px;
        }
        .user-info {
            font-size: 18px;
            margin-bottom: 8px;
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
    </style>
</head>
<header>
    <a class="Admin-header" href="#">Welcome Admin</a>
    <nav class="headeritems">
        <a href='admin1'>Home</a>
    </nav>
</header>
<body>
    <div class="container">
        <h1>List of Users</h1>
        <div id="userList">
            <% for(let x=0; x<arr.length; x++) { %>
                <div class="user-card">
                    <div class="user-info">
                        <strong>Username:</strong> <%= arr[x].addname %>
                    </div>
                    <div class="user-info">
                        <strong>Email:</strong> <%= arr[x].addemail %>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
